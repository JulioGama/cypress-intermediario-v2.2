{
  "private": true,
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run --headless --browser edge",
    "cy:run:headless": "cypress run --headless",
    "cy:run:spec": "cypress run --spec \"cypress/e2e/**/*.cy.js\"",
    "cy:run:ci": "cypress run --headless --browser edge",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^13.6.0",
    "@faker-js/faker": "^8.4.1"
  }
}
